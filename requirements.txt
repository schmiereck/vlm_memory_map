# Hexapod Spatial Memory System
# Install with: pip install -r requirements.txt

# VLM API
groq>=0.9.0
requests>=2.31.0   # used by OllamaVlmClient (local Ollama backend)

# Image processing + map rendering
Pillow>=10.0.0

# Camera capture (laptop webcam)
opencv-python>=4.8.0

# tkinter is built into Python — no install needed

# AI2-THOR virtual environment (optional, only needed with --thor)
# 4.3.0 is the last version with reliable Windows pre-built binaries
# ai2thor==4.3.0
